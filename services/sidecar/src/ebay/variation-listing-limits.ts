/** Buyer-facing limits for new Variation listing content. Standard listings
 * keep their separate eBay title contract and must not import these values. */
export const MAX_VARIATION_GROUP_TITLE_LENGTH = 80 as const;
export const MAX_VARIATION_CARD_SELECTOR_LENGTH = 65 as const;
