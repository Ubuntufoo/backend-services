import { describe, expect, it } from 'vitest';
import type { VariationListingRevisionPlanOperation, VariationListingPublishingCheckpointRow } from '../src/index.js';
import { assertVariationListingJournalCanContinue, inspectVariationListingJournal } from '../src/index.js';
const op: VariationListingRevisionPlanOperation = { sequence_no:1, operation_key:'publish', operation_kind:'group_publish', target_ref:'g', intent_version:1, intent_digest:'a'.repeat(64), intent:{} };
const cp = (o: Partial<VariationListingPublishingCheckpointRow> = {}): VariationListingPublishingCheckpointRow => ({ checkpoint_id:'c1', revision_id:'r', operation_key:'publish', attempt_number:1, checkpoint_number:1, state:'started', observed_remote_state:null, evidence:{}, created_at:'now', ...o });
describe('publishing checkpoints',()=>{ it('requires reconciliation for started/unknown latest',()=>expect(inspectVariationListingJournal(op,[cp()]).requiresReconciliation).toBe(true)); it('accepts exact terminal resolution after unknown',()=>{ const rows=[cp(),cp({checkpoint_id:'c2',checkpoint_number:2,state:'unknown',observed_remote_state:'unknown'}),cp({checkpoint_id:'c3',attempt_number:2,checkpoint_number:1,state:'confirmed_complete',observed_remote_state:'present'})]; expect(inspectVariationListingJournal(op,rows)).toMatchObject({hasUnknownHistory:true,requiresReconciliation:false}); expect(()=>assertVariationListingJournalCanContinue(op,rows)).toThrow('terminal'); }); it('rejects terminal without exact remote evidence',()=>expect(()=>inspectVariationListingJournal({...op,operation_kind:'final_absence_verification'},[cp({state:'confirmed_complete'})])).toThrow('exact remote evidence')); });
